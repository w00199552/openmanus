There is a bug in `binary_search` in `search.py`. Some test cases in
`test_search.py` fail (specifically `test_single_element_found` and
`test_two_elements`).

Find the bug, fix it, and run `python test_search.py` to verify all tests pass.

Do not change the test file. Do not rewrite the algorithm — the fix is small.
